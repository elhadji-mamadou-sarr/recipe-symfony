{% extends 'admin/admin.html.twig' %}

{% block title %}Liste des categories!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Liste des categories ✅</h1>

    <div class="button-add mt-3 mb-3 ">
            <a class="btn btn-primary fw-bold" href="{{ path('admin.categorie.create') }}">+ Ajouter</a>
          </div>

          <table class="table" >
            <thead class="table-primary">
              <tr>
                <th scope="col">#</th>
                <th scope="col">Slug</th>
                <th scope="col">Nom</th>
                <th scope="col" class="text-end">Action</th>
              </tr>
            </thead>
            <tbody>
                {% for categorie in categories %}
                <tr>
                    <td>{{ categorie.id }}</td>
                    <td>
                        {{categorie.slug}}
                    </td>
                    <td>
                        {{categorie.name}}
                    </td>
                    <td>
                    <div class="d-flex float-end gap-3">
                        <a class="btn btn-outline-primary btn-sm" 
                           href="{{ path('admin.categorie.edit', {id : categorie.id}) }}" >Modifier
                        </a>
                        <form action="{{ path('admin.categorie.delete', {id : categorie.id}) }}" method="post">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="btn btn-outline-danger btn-sm">Supprier</button>
                        </form>
                    </div>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
          </table>

    

</div>
{% endblock %}
